---
import Layout from '../../layouts/Layout.astro';
import PromptCard from '../../components/PromptCard.astro';
import prompts from '../../data/prompts.json';
import categoriesData from '../../data/categories.json';

// categories.json uses a top-level { categories: [...] } object.
// Keep a small fallback so the page remains compatible if the JSON
// is later changed to a plain array.
const categories = categoriesData.categories || categoriesData;

const types = ['Boy', 'Girl', 'Unisex', 'Couple'];
const toolOptions = ['ChatGPT', 'Gemini', 'Nano Banana', 'Midjourney'];

const trendRank = {
  hot: 0,
  current: 1,
  rising: 2,
  seasonal: 3,
  evergreen: 4
};

const sortByCreatedAtDesc = (a, b) =>
  String(b.createdAt || '').localeCompare(String(a.createdAt || ''));

// Always show the newest prompts first by noId.
const sortedPrompts = [...prompts].sort(
  (a, b) => Number(b.noId || 0) - Number(a.noId || 0)
);

const trendingPrompts = prompts
  .filter((p) => p.trending === true)
  .sort((a, b) => {
    const rankA = trendRank[a.trendStatus] ?? 99;
    const rankB = trendRank[b.trendStatus] ?? 99;
    return rankA - rankB || sortByCreatedAtDesc(a, b);
  });

const featured = sortedPrompts.slice(0, 6);

// Category metadata comes from categories.json.
// Category names, slugs, descriptions and ratings stay in one source of truth.
const categoryMeta = new Map(
  categories.map((category) => [category.slug, category])
);

const categoryItems = categories
  .filter(
    (category) =>
      category.slug !== 'all' &&
      category.slug !== 'trending'
  )
  .map((category) => ({
    category: category.name,
    meta: category,
    count: prompts.filter((p) => p.category === category.name).length
  }))
  .filter((item) => item.count > 0)
  .sort(
    (a, b) =>
      Number(b.meta?.starRating ?? 0) -
        Number(a.meta?.starRating ?? 0) ||
      Number(a.meta?.displayOrder ?? 999) -
        Number(b.meta?.displayOrder ?? 999)
  );

const cats = categoryItems.map((item) => item.category);

// "All" and "Trending" are special filter options; the remaining
// options are the 20 real prompt categories.
const filterCategories = [
  'All',
  'Trending',
  ...categories
    .filter((category) => category.slug !== 'all' && category.slug !== 'trending')
    .map((category) => category.name)
];

// Main library order: noId ascending.
const promptLibrary = [...prompts]
  .sort((a, b) => Number(b.noId || 0) - Number(a.noId || 0))
  .slice(0, 12);
---

<Layout
  title="Free AI Image Prompts | ChatGPT, Gemini & Nano Banana Prompts | Create With Soumay"
  description="Explore free AI image prompts for couples, selfies, portraits, fashion, Navratri, vintage photography, anime and more. Copy ready-to-use prompts for ChatGPT, Gemini, Nano Banana and Midjourney."
  active="Prompts"
>

  <!-- Hero -->
  <section class="hero hero-compact">
    <div class="container">

      <div class="eyebrow">Create With Soumay Prompt Library</div>

      <h1>
        Free AI Image
        <span class="gradient-text"> Prompts</span>
      </h1>

      <p class="hero-description">
        Explore free, ready-to-use AI image prompts for realistic portraits,
        couple photos, selfies, fashion, vintage photography, anime and
        creative social content. Copy and customize prompts for your favorite
        AI image generator.
      </p>

      <form class="search" id="promptSearchForm">
        <input
          id="promptSearch"
          name="q"
          placeholder="Search couple, selfie, fashion, vintage..."
          autocomplete="off"
          aria-label="Search AI prompts"
        />

        <button type="submit">
          Search
        </button>
      </form>

    </div>
  </section>

  <!-- Library stats -->
  <section class="hero-stats-section">
    <div class="container">
      <div class="hero-stats">
        <div>
          <strong>{prompts.length}+</strong>
          <span>AI Prompts</span>
        </div>

        <div>
          <strong>{categoryItems.length}</strong>
          <span>Categories</span>
        </div>

        <div>
          <strong>Free</strong>
          <span>To Copy & Use</span>
        </div>
      </div>
    </div>
  </section>


  <!-- Browse All Categories -->
  <section class="section category-preview-section" id="browse-categories">
    <div class="container">

      <div class="section-heading">
        <div>
          <div class="eyebrow">Browse by category</div>
          <h2>Explore AI Image Prompts by Category</h2>
          <p>
            Discover free, ready-to-use AI image prompts organized by style,
            occasion and creative use case. Each category links directly to
            its complete prompt collection.
          </p>
        </div>
      </div>

      <!-- Crawlable category navigation -->
      <nav class="category-nav" aria-label="AI image prompt categories">
        {categoryItems.map(({ category, meta }) => (
          <a
            href={`/prompts/${meta?.slug || category.toLowerCase()}/`}
            title={meta?.title || `${category} AI image prompts`}
          >
            {category}
          </a>
        ))}
      </nav>

      <!-- Prompt Library Filters + Results -->
      <div class="prompt-library-section" id="prompt-library">

        <div class="prompt-library-heading">
          <div>
            <div class="eyebrow">Filter prompts</div>
            <h2>Find the Right AI Image Prompt</h2>
            <p>
              Filter the prompt library by category and type to find the
              exact AI image prompts you want to create.
            </p>
          </div>

          <div class="results-note" id="promptResultsNote" aria-live="polite">
            Showing 12 of {prompts.length} prompts
          </div>
        </div>

        <div class="toolbar" aria-label="Prompt filters">

          <div class="filter-group">
            <div class="filter-label">Category</div>
            <div
              class="filters"
              id="categoryFilters"
              role="group"
              aria-label="Filter prompts by category"
            >
              {filterCategories.map((category) => (
                <button
                  type="button"
                  class:list={{ 'active-filter': category === 'All' }}
                  data-category-filter={category}
                  aria-pressed={category === 'All' ? 'true' : 'false'}
                >
                  {category}
                </button>
              ))}
            </div>
          </div>

          <div class="filter-group">
            <div class="filter-label">Type</div>
            <div
              class="filters"
              id="typeFilters"
              role="group"
              aria-label="Filter prompts by type"
            >
              {['All', ...types].map((type) => (
                <button
                  type="button"
                  class:list={{ 'active-filter': type === 'All' }}
                  data-type-filter={type}
                  aria-pressed={type === 'All' ? 'true' : 'false'}
                >
                  {type}
                </button>
              ))}
            </div>
          </div>

        </div>

        <div
          class="grid prompt-library-grid"
          id="promptLibraryGrid"
          aria-live="polite"
        >
          {promptLibrary.map((p, index) => (
            <div
              class="prompt-library-item"
              data-prompt-category={p.category || ''}
              data-prompt-type={p.type || ''}
              data-prompt-trending={p.trending === true ? 'true' : 'false'}
              data-prompt-noid={String(p.noId ?? '')}
              hidden={index >= 12}
            >
              <PromptCard p={p} />
            </div>
          ))}
        </div>

        <div class="prompt-library-actions">
          <button
            type="button"
            class="load-more-button"
            id="loadMorePrompts"
          >
            Load More
          </button>
        </div>

        <p class="prompt-library-empty" id="promptLibraryEmpty" hidden>
          No prompts match these filters yet. Try another category or type.
        </p>

      </div>


    </div>
  </section>

  <!-- CREATOR TOOLS -->

  <section class="section prompt-related-section">

    <div class="container">

      <div class="section-heading">
        <div>
          <div class="eyebrow">Create beyond the prompt</div>
          <h2>More Tools for Visual Creators</h2>
          <p>
            Pair AI image prompts with creator resources, Instagram ideas and hashtag research.
          </p>
        </div>
      </div>

      <div class="prompt-related-links">
        <a href="/creator-hub/">
          <strong>Instagram Creator Hub</strong>
          <span>Reel ideas, growth resources and creator strategy.</span>
        </a>

        <a href="/trends/">
          <strong>AI Photo Trends</strong>
          <span>Explore trending AI photo styles and learn how to create them.</span>
        </a>

        <a href="/ai-photo-guides/">
          <strong>AI Photo Guides</strong>
          <span>Learn practical techniques for better, more consistent AI images.</span>
        </a>

        <a href="/creator-tools/trending-hashtags/">
          <strong>Trending Instagram Hashtags</strong>
          <span>Find curated hashtag sets by topic for posts and Reels.</span>
        </a>

        <a href="/blog/">
          <strong>Creator Guides</strong>
          <span>Practical articles about AI, Instagram and content growth.</span>
        </a>
      </div>

    </div>

  </section>


  <!-- SEO Content -->
  <section class="section seo-section">

    <div class="container narrow">

      <div class="eyebrow">
        About the library
      </div>

      <h2>
        Detailed AI image prompts for creative projects
      </h2>

      <p>
        Create With Soumay is a growing library of ready-to-use AI image prompts
        designed to help creators turn visual ideas into clearer image
        generation instructions. Instead of starting with a blank prompt,
        you can explore structured concepts for portraits, selfies,
        couples, fashion, travel, vintage photography, anime and more.
      </p>

      <p>
        Each prompt is designed around practical visual details such as
        composition, subject direction, lighting, environment, camera
        perspective, mood and realism. When a reference image is involved,
        prompts can also include explicit instructions for preserving the
        subject's recognizable identity and natural proportions.
      </p>

      <p>
        You can copy a prompt directly from Create With Soumay, customize the
        details for your own concept and experiment with different
        variations. For better results, change one major element at a time
        instead of rewriting the entire prompt after every generation.
      </p>

      <p>
        Whether you're creating an Instagram Reel, experimenting with AI
        photography or simply looking for a new visual concept, the
        Create With Soumay prompt library gives you a practical starting point.
        After choosing an image concept, you can explore the
        <a href="/creator-hub/">Instagram Creator Hub</a> for content ideas
        or use our <a href="/creator-tools/trending-hashtags/">trending
        Instagram hashtags</a> tool to research curated hashtag sets for
        your next post or Reel.
      </p>

    </div>

  </section>


  <!-- CTA -->
  <section class="section">

    <div class="container">

      <div class="library-cta">

        <div class="eyebrow">
          Create something new
        </div>

        <h2>
          Find a prompt. Make it yours.
        </h2>

        <p>
          Copy a detailed prompt, customize the concept and create your
          next AI image.
        </p>

        <a
          href="/blog/"
          class="primary"
        >
          Read Creator Guides →
        </a>

      </div>

    </div>

  </section>


  <!-- Prompt library structured data -->
  <script>
    (() => {
      const grid = document.getElementById('promptLibraryGrid');
      const loadMoreButton = document.getElementById('loadMorePrompts');
      const emptyState = document.getElementById('promptLibraryEmpty');
      const resultsNote = document.getElementById('promptResultsNote');
      const categoryButtons = document.querySelectorAll('[data-category-filter]');
      const typeButtons = document.querySelectorAll('[data-type-filter]');

      if (!grid || !loadMoreButton) return;

      const items = Array.from(
        grid.querySelectorAll('.prompt-library-item')
      );
      const pageSize = 12;
      let visibleCount = pageSize;
      let selectedCategory = 'All';
      let selectedType = 'All';

      const normalize = (value) =>
        String(value || '').trim().toLowerCase();

      const matchesFilters = (item) => {
        const category = item.dataset.promptCategory || '';
        const type = item.dataset.promptType || '';
        const trending = item.dataset.promptTrending === 'true';

        const categoryMatches =
          selectedCategory === 'All' ||
          (selectedCategory === 'Trending'
            ? trending
            : normalize(category) === normalize(selectedCategory));

        const typeMatches =
          selectedType === 'All' ||
          normalize(type) === normalize(selectedType);

        return categoryMatches && typeMatches;
      };

      const updateButtons = (buttons, selected, attribute) => {
        buttons.forEach((button) => {
          const active = button.dataset[attribute] === selected;
          button.classList.toggle('active-filter', active);
          button.setAttribute('aria-pressed', String(active));
        });
      };

      const render = () => {
        const matchingItems = items.filter(matchesFilters);

        items.forEach((item) => {
          item.hidden = true;
        });

        matchingItems.slice(0, visibleCount).forEach((item) => {
          item.hidden = false;
        });

        const shownCount = Math.min(visibleCount, matchingItems.length);

        if (resultsNote) {
          resultsNote.textContent =
            matchingItems.length === 0
              ? '0 prompts found'
              : `Showing ${shownCount} of ${matchingItems.length} prompts`;
        }

        if (emptyState) {
          emptyState.hidden = matchingItems.length !== 0;
        }

        loadMoreButton.hidden = shownCount >= matchingItems.length;
      };

      categoryButtons.forEach((button) => {
        button.addEventListener('click', () => {
          selectedCategory = button.dataset.categoryFilter || 'All';
          visibleCount = pageSize;
          updateButtons(
            categoryButtons,
            selectedCategory,
            'categoryFilter'
          );
          render();
        });
      });

      typeButtons.forEach((button) => {
        button.addEventListener('click', () => {
          selectedType = button.dataset.typeFilter || 'All';
          visibleCount = pageSize;
          updateButtons(typeButtons, selectedType, 'typeFilter');
          render();
        });
      });

      loadMoreButton.addEventListener('click', () => {
        visibleCount += pageSize;
        render();
      });

      render();
    })();
  </script>

  <script
    type="application/ld+json"
    set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'CollectionPage',
      name: 'Free AI Image Prompts | ChatGPT, Gemini & Nano Banana Prompts',
      description:
        "Explore free AI image prompts for couples, selfies, portraits, fashion, Navratri, vintage photography, anime and more. Copy ready-to-use prompts for ChatGPT, Gemini, Nano Banana and Midjourney.",
      url: 'https://www.createwithsoumay.store/prompts/',
      isPartOf: {
        '@type': 'WebSite',
        name: 'Create With Soumay',
        url: 'https://www.createwithsoumay.store'
      },
      publisher: {
        '@type': 'Organization',
        name: 'Create With Soumay',
        url: 'https://www.createwithsoumay.store',
        logo: 'https://www.createwithsoumay.store/images/createloom-logo.png'
      },
      mainEntity: {
        '@type': 'ItemList',
        name: 'AI Image Prompt Library',
        numberOfItems: prompts.length,
        itemListOrder: 'https://schema.org/ItemListOrderAscending'
      },
      about: [
        'AI image prompts',
        'AI photography prompts',
        'portrait prompts',
        'couple photo prompts',
        'Instagram visual content'
      ],
      numberOfItems: prompts.length
    })}
  />

  <script
    type="application/ld+json"
    set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'BreadcrumbList',
      itemListElement: [
        {
          '@type': 'ListItem',
          position: 1,
          name: 'Home',
          item: 'https://www.createwithsoumay.store/'
        },
        {
          '@type': 'ListItem',
          position: 2,
          name: 'AI Image Prompts',
          item: 'https://www.createwithsoumay.store/prompts/'
        }
      ]
    })}
  />

</Layout>

<style>

  .hero-description {
    max-width: 720px;
    margin: 0 auto;
    color: var(--muted, #a1a1aa);
    line-height: 1.8;
  }

  .hero-stats-section {
    padding: 0 0 10px;
  }

  .hero-stats {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 40px;
    margin-top: 30px;
  }

  .hero-stats div {
    display: flex;
    flex-direction: column;
    gap: 3px;
    text-align: center;
  }

  .hero-stats strong {
    color: #fff;
    font-size: 1.15rem;
  }

  .hero-stats span {
    color: var(--muted, #a1a1aa);
    font-size: 0.75rem;
  }

  .section-heading {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 25px;
  }

  .section-heading h2 {
    margin: 7px 0 5px;
  }

  .section-heading p {
    margin: 0;
    color: var(--muted, #a1a1aa);
    line-height: 1.6;
  }

  .text-link {
    color: #c4b5fd;
    text-decoration: none;
    font-weight: 700;
    white-space: nowrap;
  }

  .featured-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 15px;
  }

  .featured-card {
    display: block;
    padding: 21px;
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 17px;
    background:
      linear-gradient(
        145deg,
        rgba(139,92,246,0.08),
        rgba(255,255,255,0.025)
      );
    color: inherit;
    text-decoration: none;
    transition:
      transform 0.2s ease,
      border-color 0.2s ease;
  }

  .featured-card:hover {
    transform: translateY(-3px);
    border-color: rgba(139,92,246,0.4);
  }

  .featured-card > span {
    color: #a78bfa;
    font-size: 0.7rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .featured-card h3 {
    margin: 10px 0;
    color: #fff;
    line-height: 1.35;
  }

  .featured-card p {
    color: var(--muted, #a1a1aa);
    font-size: 0.88rem;
    line-height: 1.65;
  }

  .featured-card strong {
    color: #c4b5fd;
    font-size: 0.82rem;
  }

  .category-preview-section {
    background: rgba(255,255,255,0.015);
  }

  .category-nav {
    display: flex;
    gap: 9px;
    flex-wrap: wrap;
    margin: 0 0 42px;
    padding: 15px;
    border: 1px solid rgba(255,255,255,0.07);
    border-radius: 17px;
    background: rgba(255,255,255,0.02);
  }

  .category-nav a {
    display: inline-flex;
    align-items: center;
    min-height: 34px;
    padding: 7px 12px;
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 999px;
    color: #d4d4d8;
    text-decoration: none;
    font-size: .76rem;
    font-weight: 700;
    transition: border-color .2s ease, background .2s ease, color .2s ease;
  }

  .category-nav a:hover {
    border-color: rgba(139,92,246,.4);
    background: rgba(139,92,246,.08);
    color: #c4b5fd;
  }

  .category-preview-list {
    display: grid;
    gap: 58px;
  }

  .category-preview {
    scroll-margin-top: 90px;
  }

  .category-preview-heading {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 22px;
    margin-bottom: 20px;
  }

  .category-preview-heading h3 {
    margin: 7px 0 6px;
    font-size: clamp(1.25rem, 2vw, 1.65rem);
    line-height: 1.3;
  }

  .category-preview-heading h3 a {
    color: #fff;
    text-decoration: none;
  }

  .category-preview-heading h3 a:hover {
    color: #c4b5fd;
  }

  .category-preview-heading p {
    max-width: 760px;
    margin: 0;
    color: var(--muted, #a1a1aa);
    line-height: 1.65;
  }

  .category-view-more {
    flex: 0 0 auto;
    color: #c4b5fd;
    text-decoration: none;
    font-size: .82rem;
    font-weight: 800;
    white-space: nowrap;
  }

  .category-view-more:hover {
    text-decoration: underline;
  }

  .category-prompt-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
    align-items: stretch;
  }

  .category-empty {
    padding: 22px;
    border: 1px solid rgba(255,255,255,.07);
    border-radius: 15px;
    color: var(--muted, #a1a1aa);
  }

  .category-empty a {
    color: #c4b5fd;
    font-weight: 700;
    text-decoration: none;
  }

  .prompt-library-section {
    margin: 0 0 62px;
  }

  .prompt-library-heading {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 24px;
  }

  .prompt-library-heading h2 {
    margin: 7px 0 5px;
  }

  .prompt-library-heading p {
    max-width: 760px;
    margin: 0;
    color: var(--muted, #a1a1aa);
    line-height: 1.6;
  }

  .prompt-library-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
    align-items: stretch;
  }

  .prompt-library-item[hidden] {
    display: none !important;
  }

  .prompt-library-actions {
    display: flex;
    justify-content: center;
    margin-top: 28px;
  }

  .load-more-button {
    min-height: 42px;
    padding: 10px 20px;
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 999px;
    background: rgba(255,255,255,0.03);
    color: #fff;
    font: inherit;
    font-size: .82rem;
    font-weight: 800;
    cursor: pointer;
    transition: border-color .2s ease, background .2s ease, transform .2s ease;
  }

  .load-more-button:hover {
    border-color: rgba(139,92,246,.45);
    background: rgba(139,92,246,.1);
    transform: translateY(-1px);
  }

  .load-more-button:focus-visible,
  .filters button:focus-visible {
    outline: 2px solid #a78bfa;
    outline-offset: 2px;
  }

  .prompt-library-empty {
    padding: 42px 20px;
    margin: 22px 0 0;
    border: 1px solid rgba(255,255,255,.07);
    border-radius: 15px;
    color: var(--muted, #a1a1aa);
    text-align: center;
  }

  .toolbar {
    display: grid;
    gap: 16px;
    margin-bottom: 24px;
  }

  .filter-group {
    display: grid;
    gap: 8px;
  }

  .filter-label {
    color: #85858f;
    font-size: .68rem;
    font-weight: 850;
    letter-spacing: .07em;
    text-transform: uppercase;
  }

  .filters {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }

  .filters button {
    min-height: 36px;
    padding: 7px 13px;
    border: 1px solid rgba(255,255,255,0.10);
    border-radius: 999px;
    background: rgba(255,255,255,0.035);
    color: #d4d4d8;
    font: inherit;
    font-size: .76rem;
    font-weight: 700;
    line-height: 1;
    cursor: pointer;
    transition:
      background .2s ease,
      border-color .2s ease,
      color .2s ease,
      transform .2s ease;
  }

  .filters button:hover {
    border-color: rgba(139,92,246,.38);
    background: rgba(139,92,246,.08);
    color: #c4b5fd;
    transform: translateY(-1px);
  }

  .filters button.active-filter {
    border-color: rgba(139,92,246,.55) !important;
    background: rgba(139,92,246,.14) !important;
    color: #c4b5fd !important;
  }

  .active-filter {
    border-color: rgba(139,92,246,0.5) !important;
    background: rgba(139,92,246,0.13) !important;
    color: #c4b5fd !important;
  }

  .results-note {
    color: var(--muted, #a1a1aa);
    font-size: 0.82rem;
  }

  .empty-state {
    padding: 65px 20px;
    text-align: center;
  }

  .empty-icon {
    font-size: 2rem;
    color: #a78bfa;
  }

  .empty-state h3 {
    margin: 10px 0 5px;
  }

  .empty-state p {
    color: var(--muted, #a1a1aa);
    margin-bottom: 18px;
  }

  .narrow {
    max-width: 850px;
  }

  .seo-section {
    background: rgba(255,255,255,0.015);
  }

  .seo-section h2 {
    margin: 8px 0 20px;
  }

  .seo-section p {
    color: #c7c7cf;
    line-height: 1.85;
    font-size: 1rem;
  }

  .seo-section a {
    color: #c4b5fd;
    font-weight: 700;
    text-decoration: none;
  }

  .seo-section a:hover {
    text-decoration: underline;
  }

  /* Related creator tools */

  .prompt-related-section {
    background: rgba(255,255,255,0.015);
  }

  .prompt-related-links {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
  }

  .prompt-related-links a {
    display: flex;
    flex-direction: column;
    gap: 7px;
    min-height: 120px;
    padding: 18px;
    border: 1px solid rgba(255,255,255,0.07);
    border-radius: 15px;
    background: rgba(255,255,255,0.02);
    color: inherit;
    text-decoration: none;
    transition: transform .2s ease, border-color .2s ease;
  }

  .prompt-related-links a:hover {
    transform: translateY(-2px);
    border-color: rgba(139,92,246,0.35);
  }

  .prompt-related-links strong {
    color: #fff;
    font-size: .88rem;
  }

  .prompt-related-links span {
    color: var(--muted, #a1a1aa);
    font-size: .78rem;
    line-height: 1.55;
  }

  .library-cta {
    padding: 45px 25px;
    text-align: center;
    border: 1px solid rgba(139,92,246,0.2);
    border-radius: 22px;
    background:
      linear-gradient(
        145deg,
        rgba(139,92,246,0.12),
        rgba(255,255,255,0.025)
      );
  }

  .library-cta h2 {
    margin: 8px 0 10px;
  }

  .library-cta p {
    max-width: 620px;
    margin: 0 auto;
    color: var(--muted, #a1a1aa);
    line-height: 1.7;
  }

  .library-cta .primary {
    display: inline-flex;
    margin-top: 20px;
    text-decoration: none;
  }

  @media (max-width: 900px) {

    .featured-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .category-prompt-grid,
    .prompt-library-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

  }

  @media (max-width: 600px) {

    .prompt-related-links {
      grid-template-columns: 1fr;
    }

    .hero-stats {
      gap: 25px;
    }

    .section-heading {
      align-items: flex-start;
      flex-direction: column;
    }

    .featured-grid,
    .category-prompt-grid,
    .prompt-library-grid {
      grid-template-columns: 1fr;
      gap: 14px;
    }

    .prompt-library-heading {
      align-items: flex-start;
      flex-direction: column;
    }

    .category-preview-heading {
      align-items: flex-start;
      flex-direction: column;
      gap: 10px;
    }

    .category-preview-heading p {
      font-size: .92rem;
    }

    .category-preview {
      padding-top: 8px;
    }

    .featured-card {
      padding: 18px;
    }

  }

</style>
